import react, { useContext } from "react"
import useFetch from "hooks/useFetch"

import { DataContext } from "../../context"



const RandomMeal = () => {

          const [data] = useFetch("random.php")
          const fetchData = useContext(DataContext)
          console.log("Random", fetchData.fetchData)

          return (
                    <div className="meal">

                              <div className="meal_header">
                                        <span className="random">Random recipe</span>
                                        <img className="img_btn" data-index="1" src={fetchData.fetchData.strMealThumb}></img>
                              </div>
                              <div className="meal_body">
                                        <h3>{fetchData.fetchData.strMeal}</h3>

                              </div>
                    </div>
          )
}

export default RandomMeal